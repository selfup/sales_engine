# store the fields as big decimals
# store the id's as fixnum
# do not compare updated at and created at
# isolate date part
# return collections of things as arrays not hashes
# find_all_by needs to be an array or an empty array IF NOTHING
# find_by_id(something)
# make it an alias
# put the function into a module
# module is called FindBy
# get a thing, sub these two things
